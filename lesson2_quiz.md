# lesson2_quiz

第三节课课前热身。复习 L2 的猜数字游戏、循环，以及用循环处理股票数据。

## 1. `while` 最接近下面哪句话？

A. 只执行一次  
B. 只要条件成立，就重复执行  
C. 随机执行  
D. 专门用来打印内容

## 2. 下面这句代码是什么意思？

```python
while guess != answer:
```

A. 只要猜对了，就继续猜  
B. 只要还没猜对，就继续猜  
C. 把 `guess` 改成 `answer`  
D. 随机生成一个答案

## 3. `random.randint(1, 10)` 会生成什么？

A. 1 到 10 之间的整数  
B. 1 到 10 之间的小数  
C. 只能生成 1 或 10  
D. 生成一个字符串

## 4. 计数器通常怎么加一？

```python
count = 1
```

A. `count + 1`  
B. `count = count + 1`  
C. `count == count + 1`  
D. `count = 1 + 1`

## 5. 这段代码里，哪一行负责把用户输入转成整数？

```python
guess = int(input("guess: "))
```

A. `guess`  
B. `input("guess: ")`  
C. `int(...)`  
D. `"guess: "`

## 6. 下面这组数据里，`stock[0]` 是什么？

```python
stock = ["贵州茅台", 1709, 1705]
```

A. `"贵州茅台"`  
B. `1709`  
C. `1705`  
D. `0`

## 7. 这段代码会循环几次？

```python
stocks = [
    ["贵州茅台", 1709, 1705],
    ["招商银行", 35.20, 36.00],
    ["宁德时代", 180.00, 174.00],
]

for stock in stocks:
    print(stock[0])
```

A. 1 次  
B. 2 次  
C. 3 次  
D. 不确定

## 8. 循环处理股票数据，最重要的前提是什么？

A. 每条数据结构相同  
B. 每只股票价格相同  
C. 每只股票都上涨  
D. 每只股票名字一样长

## 9. 哪只股票跌的钱最多？

```text
贵州茅台：1709 -> 1705
招商银行：35.20 -> 36.00
宁德时代：180.00 -> 174.00
```

A. 贵州茅台  
B. 招商银行  
C. 宁德时代  
D. 无法判断

## 老师参考

1. B
2. B
3. A
4. B
5. C
6. A
7. C
8. A
9. C
