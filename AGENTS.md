# Python Course 101 项目规则

本文件记录本仓库课程改版的项目级硬规则。适用于 Codex、Claude 和其他参与课程内容编辑的 agent。

## 课程定位

- 目标学员是夜校非代码专业同学，默认下班后精力有限。
- 课程目标不是训练程序员，而是让学员能看懂简单脚本、手改少量代码、理解基础数据分析概念，并知道如何用 AI 辅助学习。
- 课程内容优先使用仓库现有资源；新增文件、依赖、外部数据源都要谨慎。

## 内容密度

- 每节课学生跟敲代码必须短、少、可预测。
- 不要为了显得完整而一次塞入太多新概念。
- 面向学生的课件和讲稿只写本节课要讲/要做的内容；不要主动提不希望学员关注的岔路，因为很多人本来没想到，提了反而会记住。
- L1 学生可见代码只围绕：变量、`=`、数字、字符串、基础加减乘除、函数定义、参数、函数调用、`return`、`print()`。
- L1 不引入 `DataFrame`、`df`、`tail()`、`shift()`、第三方库、批量数据结构等复杂概念。
- 真实股票接口代码可以作为老师演示或 AI 测试素材，但不能当作 L1 学生跟敲主线。

## L1 改版边界

- 前半基于现有 `lesson1 copy.key`，正式教学从圆面积例子开始，到判断奇偶数、知识回顾结束。
- L1 的圆面积和判断奇偶数不要脱离函数概念；函数、参数、调用、返回值从第一节课开始反复强化，但第一晚只要求学员见过和能指出来。
- PyCharm 初始化采用老师投屏实际演示和巡场解决问题，不做成 Keynote 截图流程。
- L1 前半可以保留现有介绍和回顾内容，但不进入循环部分。
- L1 后半使用三个股票/数据分析小例子，每个例子遵循“代码识别 -> 业务问题 -> 标准答案/知识点”的结构。
- 每个例子里代码页、问题页、答案页分开，不把所有内容挤在一页。
- AI 环节先作为集中演示或课后学习方法，不默认塞进每个例子。

## 投影和字号

- 教室投影按 1080p 设计，现有 L1 导出尺寸为 1920×1080。
- 代码展示不能为了塞内容缩到默认 14 号；PyCharm/命令行现场展示代码建议至少 18-20。
- Keynote 代码块也按 18-20 号现场可读标准设计。
- 单页代码尽量控制在 8-12 行，超过优先拆页，不缩字号。
- `.py` 源文件里的课程示例可以参考 L6/L7 的旧经验：一个函数控制在一满屏可讲完的长度，通常约 10-18 行逻辑代码。
- 写课程 `.py` 时优先按“一个函数/一个任务/一屏讲完”拆分，避免课堂讲解一个例子时需要在源码里滚动多屏。
- 问题页一页只放 2-3 个短问题。
- 答案页只放关键输出和一句结论，不放长日志。

## 工作流

- 课程改版的长期记忆维护在 `docs/memory/MEMORY.md`。改课程 Markdown / HTML 前，先读该索引和相关 memory 文件。
- 用户对内容风格的反复纠正，要沉淀进 `docs/memory/`，不要只依赖当前 session 上下文。
- Markdown 是课程内容底稿；从旧 Keynote 搬来的有效内容，优先同步进 Markdown。
- HTML 是投屏展示层；根据 Markdown 生成或维护，避免把内容基础继续锁在 PPT/Keynote 里。
- Keynote 作为旧课件参考资料，默认不继续当主编辑入口。
- 需要修改 Keynote 时先确认内容结构，避免把内容设计和版式调整混在一起。
- 课程设计稿默认放在 `docs/` 下。
- 课程面向用户的文档默认中文。
